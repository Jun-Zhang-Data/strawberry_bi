version: 2

models:
  - name: stg_membership
    description: "Bronze staging over daily full membership feed."
    columns:
      - name: member_id
        tests:
          - not_null
          - unique
      - name: status
        tests:
          - not_null
          - accepted_values:
              values: ['BRONZE','SILVER','GOLD','PLATINUM']
